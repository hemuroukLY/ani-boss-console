# ANI BOSS Console 当前状态

> 本文件是项目当前范围、数据接入状态、主要缺口和简短开发记录的集中入口。产品与接口事实仍以对应 GitNexus 索引为准。

## 当前口径

- 项目：独立 ANI 平台管理端前端；仓库根目录已经代表 BOSS 范围，源码按业务语义命名。
- 产品原型：GitNexus 索引 `产品原型-9.03`；信息架构、菜单、路由和页面交互以该版本为准。
- 后端：独立 ANI 仓库；接口契约与行为以 GitNexus 索引 `ANI` 为准。
- 应用骨架：与 `ani-console` 保持一致，顶部承载一级菜单，左侧承载二级菜单和三级分组。
- UI：Arco Design React 为基础组件库，Tailwind CSS 负责布局，ECharts 负责图表。
- 数据：当前尚无统一 API 客户端或后端类型快照，页面主要使用前端模型和演示状态。
- 验证：页面与交互默认由项目负责人手动验证；Agent 不主动构建或启动应用。

## 当前模块

### 平台运营总览

- 运营总览：运营指标、告警摘要与待处理事项。
- 资源池与容量态势：区域容量与资源状态。
- GPU 资源池态势：GPU 设备、使用率与状态展示。
- AI 服务运营态势：推理服务指标、趋势和异常服务。
- 知识库运营态势：知识库规模、查询趋势和索引状态。
- 平台告警与待处理：平台告警列表及演示处置状态。

### 租户管理

- 租户列表与租户详情。
- 配额策略列表与详情。
- 租户管理员列表与详情，以及邀请、角色和密码相关弹窗。
- 租户计费与用量列表、详情和账务记录。
- 租户生命周期、身份安全、配额使用与操作历史等详情区域。

### 资源池与基础设施

- 平台资源池总览。
- GPU 资源池管理，包括设备表、拆分、配额和恢复等演示操作。
- 节点状态，位于资源池菜单下，包括区域、资源池、健康状态和关键字筛选。
- 存储基础设施，位于基础设施菜单下，展示块、对象、文件和向量存储后端的健康与容量状态、StorageClass 运营和最近平台事件。
- 租户存储配额，位于基础设施菜单下，顶部保留汇总指标，下方由租户热度 TopN、租户存储配额和最近事件三个板块组成。
- 网络基础设施，位于基础设施菜单下，展示 SDN 控制器、出口网关、IPAM、网络组件和 IP 地址池运行状态。
- 镜像仓库运维，位于资源池与基础设施菜单下，包括租户镜像配额、漏洞扫描和垃圾回收三个静态页面；支持配额状态、镜像风险、用途和关键字筛选，后台操作以禁用态保留。

### 运维与可观测

- 平台健康：汇总控制面和数据面组件的正常、降级、异常状态，展示依赖检查并提供指标、日志和 Trace 下钻入口。
- GPU 监控、推理监控和知识库监控：使用统一监控骨架展示领域指标、趋势图和当前关注项。
- 组件指标：支持切换平台组件，展示 P99、错误率、就绪副本、请求量和依赖探测结果。
- 日志与 Trace：提供静态日志组合筛选、调用链样本切换、瀑布图和耗时排行。
- 告警规则：展示平台规则阈值、级别、启停状态和通知渠道；后台写操作保持禁用。
- 运维 Skills、任务历史和故障处理：分别展示运维能力、异步作业记录和平台故障清单，支持领域、状态、类型、级别和关键字筛选；执行与处置操作保持禁用。

### 平台计量与结算

- 计量总览：按 GPU-Hours、CPU-Hours、Memory、Storage、Tokens 和 KB Queries 六个原型页签展示本月合计、环比、峰值日、配额使用率、近 7 日趋势与租户用量排行；支持区域和租户关键字筛选，导出操作保持禁用。
- 原型菜单备注写有“七类指标”，但页面线框实际只定义上述六个页签；当前实现以可运行线框为准，待产品口径澄清后再补充。

### 安全审计与合规

- 平台审计日志：只读展示平台管理员与系统策略操作，支持资源类型、执行结果和关键字筛选，并可查看请求 ID、来源 IP、User-Agent 与变更摘要。
- API Key 审计：跨租户展示 Key 创建、调用、轮换和禁用事件，仅显示不可逆前缀与权限范围，不提供密钥揭示操作。
- 推理调用审计：展示租户、服务、模型、接口、Key 前缀、状态码、耗时、Token、来源 IP 和请求 ID，不记录提示词或响应正文。
- 合规导出与取证：提供范围、内容和确认三步静态向导，并展示最近取证任务；真实生成、下载和自定义时间范围尚未开放。

### 平台设置

- 平台运营账号：展示管理端本地登录身份、角色、状态、MFA 和最近登录信息，支持角色、状态和关键字筛选以及只读详情；账号不属于租户，也不会同步为租户成员。
- 登录与 IdP：P1 预留页，展示 OIDC/SAML 接入结构、当前登录方式和接入边界，所有配置与连接测试操作保持禁用。
- 会话与安全策略：P2 预留页，展示会话超时、并发会话、重新认证、MFA、密码长度、失败锁定和来源 IP 白名单等配置结构，当前展示值不代表服务端生效策略。

### 平台集成与通知

- 运维 Webhook：展示平台级事件订阅、签名密钥前缀、启停状态和最近投递结果，支持只读详情及投递日志查看；新增、测试、密钥重置和启停操作保持禁用。
- 企业通知集成：P1 静态列表，展示企业微信、钉钉、邮件和飞书等通知渠道、事件范围、状态与投递概况，不提供真实凭据配置或测试通知。
- 运营系统对接：P1 静态列表，展示 CMDB、ITSM、财务和数据平台等对接关系、数据方向、对象范围与同步概况；页面示意字段不作为接口契约。

产品原型 `产品原型-9.03` 中 BOSS 端 8 个一级菜单、41 个可点击页面均已形成对应静态路由；后续新增范围仍须先更新并查询产品原型。

## 当前缺口与整理重点

- 尚未接入真实 ANI API；前端演示数据不能作为接口契约依据。
- `ops-gpu/index.tsx` 已超过 800 行，`ops-pool/index.tsx` 以及若干租户管理路由也超过建议规模，应按独立表格、弹窗和业务流程逐步拆分。
- 部分业务状态仍集中在 Provider 与模型文件中；接入真实数据时需明确服务端状态、页面状态和演示状态的边界。
- `src/features/` 已不再作为目标目录；遗留内容应在相关功能调整中迁移到 `src/components/<scope>/`，不得继续扩展。
- 当前静态页面已覆盖产品原型中的全部 BOSS 导航入口；接入真实数据、写操作和跨页闭环时仍需逐项查询 ANI 后端契约，不得将演示数据作为接口依据。

## 近期记录

| 日期 | 摘要 |
|------|------|
| 2026-09-04 | 将页面信息架构与交互规格基线统一更新为 GitNexus 索引 `产品原型-9.03`，并完成全量覆盖审计：BOSS 端 8 个一级入口、41 个可点击页面均已形成唯一静态路由并进入自动生成路由树，另保留 5 个租户详情路由。 |
| 2026-09-04 | 依据产品原型补齐镜像仓库运维、运维与可观测、平台计量与结算、安全审计与合规、平台设置及平台集成与通知页面，覆盖静态筛选、趋势与指标、日志与 Trace、只读详情、敏感字段掩码和取证向导等展示能力；真实数据、导出、执行、配置及其他写操作尚未接入 ANI API，均保持静态或禁用，其中计量沿用线框六页签并保留“七类指标”口径待确认。已生成路由树；GitNexus 变更检测为低风险且无受影响执行流，`git diff --check` 通过，未运行构建或启动应用。 |
| 2026-09-02 | 将页面信息架构与交互规格基线更新为 GitNexus 索引 `产品原型-9.01`；统一规定用户界面空值占位使用半角 `-`，不得使用长破折号 `—`。 |
| 2026-09-01 | 依据产品原型新增“网络基础设施”页面并归入基础设施菜单，顶部展示 SDN 控制器、出口网关和最高 IP 池占用汇总，下方展示网络组件与 IP 地址池明细；支持区域、健康状态和关键字筛选，不提供模拟状态操作，尚未接入 ANI 网络基础设施接口。 |
| 2026-09-01 | 依据产品原型新增“租户存储配额”页面并归入基础设施菜单，页面由租户热度 TopN、租户存储配额和最近事件三个板块组成；配额明细覆盖块存储、对象容量与对象数、NFS、向量、IOPS、带宽和待扩容申请，支持状态范围与租户关键字筛选，不提供调额或审批演示操作，尚未接入 ANI 存储配额接口。 |
| 2026-09-01 | 将行操作、名称单元格和行操作按钮的实现与样式统一迁入 `DataTable`，并分别重命名为 `DataTableRowActions`、`DataTableNameCell` 和 `DataTableRowActionButton`；同步更新所有调用，移除 `ListDataTable` 和独立名称单元格中的重复实现，`ListDataTable` 仅保留列表表格包装及“更多”组合。 |
| 2026-09-01 | 依据产品原型新增“存储基础设施”页面并归入基础设施菜单，展示 4 类存储后端的健康汇总、综合利用率和明细，并补充 StorageClass 运营与最近平台事件；支持类型、状态、区域和关键字筛选，不加入原型中的模拟状态变更、StorageClass 管理或扩池操作，尚未接入 ANI 存储接口。 |
| 2026-09-01 | 依据产品原型新增“节点状态”页面并归入资源池菜单，汇总仅展示全部 8、Ready 5、NotReady 3，明细展示节点健康、资源规格、GPU 调度量、工作负载、标签、污点和最后心跳；支持组合筛选与刷新，不提供演示状态修改操作，尚未接入 ANI 节点接口。已生成路由树并执行 GitNexus 变更检测与 `git diff --check`，未运行构建或启动应用。 |
| 2026-08-31 | 将 `src/components/platform-overview/` 重命名为 `src/components/overview/`，将 `src/components/tenant-management/` 重命名为 `src/components/tenant/`，并同步更新全部源码导入路径。 |
| 2026-08-31 | 按组件拆分约定将租户管理员、租户计费和配额策略的列表与详情实现迁入对应领域组件，路由仅保留注册、参数读取和组件组合；移除租户相关列表的前端本地搜索与筛选逻辑，列表直接展示当前数据源。 |
| 2026-08-31 | 收敛 `AGENTS.md` 为开发入口和强制规则摘要，工程与 UI 细则分别引用 `docs/CONVENTIONS.md` 和 `docs/UI-CONVENTIONS.md`；组件拆分规则继续仅保留在 `AGENTS.md`。 |
| 2026-08-31 | 依据 `ani-console` 文档结构、当前源码和 `AGENTS.md`，建立文档索引、工程约定、UI 开发约定与项目状态文档，并更新根 README。 |
| 2026-08-31 | 明确组件拆分的规模参考、适合拆分信号和不应机械拆分的情形。 |
| 2026-08-31 | 项目重新建立 GitNexus 索引；`AGENTS.md` 与 `CLAUDE.md` 中的 GitNexus 托管区保持由 GitNexus 管理。 |
